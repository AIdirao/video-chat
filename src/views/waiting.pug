doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title WaitingRoom
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")

    body
        header
            h1 Ai-dirao
        main            
            div#call
                div#myStream
                    video#myFace(autoplay, playsinline, width="400", height="400")
                    button#mute Mute
                    button#camera Turn Camera Off
                    select#cameras
                video#peerFace(autoplay, playsinline, width="400", height="400")
            button(id="enterMeeting") 회의 참가

        // ✅ `defer`를 사용하여 모든 스크립트가 실행되기 전에 다운로드되도록 함
        //script(src="/public/js/libs/nsfwjs.js", defer)
        script(src="https://unpkg.com/nsfwjs", defer)
        script(src="https://cdn.jsdelivr.net/npm/@mediapipe/selfie_segmentation/selfie_segmentation.js", defer)
        script(src="/socket.io/socket.io.js", defer)
        script(src="/public/js/waiting.js", defer)
